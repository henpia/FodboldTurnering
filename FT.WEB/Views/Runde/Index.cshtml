@model IEnumerable<FT.Entities.Models.Runde>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Turnering.Navn)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Betegnelse)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Turnering.Navn)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Betegnelse)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RundeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RundeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RundeId })
        </td>
    </tr>
}

</table>
