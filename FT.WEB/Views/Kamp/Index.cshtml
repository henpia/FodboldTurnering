@model IEnumerable<FT.Entities.Models.Kamp>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Runde.Betegnelse)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DatoForKamp)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Resultat)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Runde.Betegnelse)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DatoForKamp)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Resultat)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.KampId }) |
            @Html.ActionLink("Details", "Details", new { id=item.KampId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.KampId })
        </td>
    </tr>
}

</table>
