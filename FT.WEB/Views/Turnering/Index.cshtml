@model IEnumerable<FT.Entities.Models.Turnering>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Navn)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MaxAntalHold)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Navn)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MaxAntalHold)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.TurneringId }) |
            @Html.ActionLink("Details", "Details", new { id = item.TurneringId }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.TurneringId })
        </td>
    </tr>
}

</table>
