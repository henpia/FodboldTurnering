@model IEnumerable<FT.Entities.Models.Hold>

@{
    ViewBag.Title = "Index";
}

<p><h2 class="text-center">Hold</h2></p>

<br />

<p>
    <h4>Vælg et hold i listen nedenfor....</h4>
</p>

<table class="table table-dark table-striped table-bordered table-hover table-sm table-responsive-md">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Navn)
        </th>
    </tr>

@foreach (var item in Model) {
    <tr onclick="location.href='@(Url.Action("Details", "Hold", new { id = item.HoldId }))'">
        <td>
            @Html.DisplayFor(modelItem => item.Navn)
        </td>
    </tr>
}

</table>

<p>
    @Html.ActionLink("Nyt Hold", "Create", null, new { @class = "btn btn-primary" })
</p>
